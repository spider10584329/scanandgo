# ============================================
# Database Configuration
# ============================================
DATABASE_URL=mysql+pymysql://root@localhost:3306/scanandgo_prod

# ============================================
# JWT Configuration (Authentication)
# ============================================
# Generate new secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=9_sgEB5in6dEqJguJDrpmoX20rpHDZpe_cPurN5_NQM
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=12

# ============================================
# PulsePoint API Configuration
# ============================================
PULSEPOINT_API_URL=https://api.pulsepoint.clinotag.com
PULSEPOINT_PROJECT_ID=20
PULSEPOINT_API_USERNAME=admin
PULSEPOINT_API_PASSWORD=admin

# ============================================
# Server Configuration
# ============================================
HOST=0.0.0.0
PORT=8000
WORKERS=4
RELOAD=True

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
# Development: include both localhost:3000 and 127.0.0.1:3000
# Production: include your production domain(s)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001

# ============================================
# Environment
# ============================================
# Options: development, staging, production
ENVIRONMENT=development

# ============================================
# Redis/Cache Configuration (Optional)
# ============================================
# Install Redis: sudo apt install redis-server (Linux) or brew install redis (Mac)
REDIS_URL=redis://localhost:6379/0
CACHE_ENABLED=False
CACHE_TTL_SECONDS=60

# ============================================
# Logging
# ============================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# .\venv\Scripts\Activate.ps1; uvicorn app.main:app --reload --host 0.0.0.0 --port 8000